{"id":"stream-the-log","title":"Reading a log with streams","difficulty":"stretch","chapter":"streams","topics":["streams","flatmap","optional","primitive-streams"],"standard":"java21","check":"unit","stdin":"","cases":[],"timeLimitMs":0,"promptHtml":"<p>A realistic pipeline, over lines that look like this:</p>\n<figure class=\"code\"><pre class=\"shiki shiki-themes vitesse-light vitesse-dark\" style=\"--sh-light:#393a34;--sh-dark:#dbd7caee;--sh-light-bg:#ffffff;--sh-dark-bg:#121212\" tabindex=\"0\"><code><span class=\"line\"><span>2026-03-01T09:15:00 INFO  auth    login ok user=ada</span></span>\n<span class=\"line\"><span>2026-03-01T09:15:04 ERROR billing charge failed code=402</span></span></code></pre></figure>\n<p>Four fields separated by runs of whitespace — timestamp, level, component, and\na message that may itself contain spaces — then any number of <code>key=value</code>\npairs at the end of the message.</p>\n<p>Write these, all as stream pipelines:</p>\n<ul>\n<li><code>static List&lt;String&gt; levels(List&lt;String&gt; lines)</code> — the distinct levels, in\nfirst-seen order</li>\n<li><code>static Optional&lt;String&gt; firstError(List&lt;String&gt; lines)</code> — the message of the\nfirst <code>ERROR</code> line, or empty. It must not examine lines after the first\n<code>ERROR</code>.</li>\n<li><code>static Map&lt;String, Long&gt; countsByComponent(List&lt;String&gt; lines)</code> — how many\nlines each component produced</li>\n<li><code>static IntSummaryStatistics messageLengths(List&lt;String&gt; lines)</code> — statistics\nover the length of each line’s message, with no boxing</li>\n<li><code>static List&lt;String&gt; valuesOf(List&lt;String&gt; lines, String key)</code> — every value\nappearing as <code>key=value</code> anywhere in the file, in order, with duplicates kept</li>\n</ul>\n<p>Blank lines and lines with fewer than four fields are ignored everywhere.</p>\n","starter":"static String[] fieldsOf(String line) {\n    return line.trim().split(\"\\\\s+\", 4);\n}\n\nstatic List<String> levels(List<String> lines) {\n    return List.of();\n}\n\nstatic Optional<String> firstError(List<String> lines) {\n    return Optional.empty();\n}\n\nstatic Map<String, Long> countsByComponent(List<String> lines) {\n    return Map.of();\n}\n\nstatic IntSummaryStatistics messageLengths(List<String> lines) {\n    return new IntSummaryStatistics();\n}\n\nstatic List<String> valuesOf(List<String> lines, String key) {\n    return List.of();\n}","tests":"List<String> log = List.of(\n    \"2026-03-01T09:15:00 INFO  auth    login ok user=ada\",\n    \"\",\n    \"2026-03-01T09:15:04 ERROR billing charge failed code=402 user=ada\",\n    \"   \",\n    \"2026-03-01T09:15:09 WARN  auth    slow response user=grace\",\n    \"not a log\",\n    \"2026-03-01T09:15:12 ERROR auth    token expired user=grace\",\n    \"2026-03-01T09:15:20 INFO  billing invoice sent\");\n\ncheckEq(levels(log), List.of(\"INFO\", \"ERROR\", \"WARN\"));\ncheckEq(levels(List.of()), List.of());\ncheckEq(levels(List.of(\"\", \"  \", \"too short\")), List.of());\n// Four whitespace-separated fields is all this parser asks for, so a\n// four-word sentence passes. Rejecting that needs a real format check.\ncheckEq(levels(List.of(\"any four words here\")), List.of(\"four\"));\n\ncheckEq(firstError(log), Optional.of(\"charge failed code=402 user=ada\"));\ncheckEq(firstError(List.of(\"2026-01-01T00:00:00 INFO a nothing here\")), Optional.empty());\n\ncheckEq(countsByComponent(log), Map.of(\"auth\", 3L, \"billing\", 2L));\ncheckEq(countsByComponent(List.of()), Map.of());\n\nIntSummaryStatistics stats = messageLengths(log);\ncheckEq(stats.getCount(), 5L);\ncheckEq(stats.getMin(), \"invoice sent\".length());\ncheckEq(stats.getMax(), \"charge failed code=402 user=ada\".length());\n\ncheckEq(valuesOf(log, \"user\"), List.of(\"ada\", \"ada\", \"grace\", \"grace\"));\ncheckEq(valuesOf(log, \"code\"), List.of(\"402\"));\ncheckEq(valuesOf(log, \"missing\"), List.of());","hints":["<p>Start every pipeline the same way:\n<code>lines.stream().map(Main::fieldsOf).filter(f -&gt; f.length == 4)</code>. Factoring\nthat into its own helper returning a <code>Stream&lt;String[]&gt;</code> saves repeating it\nfive times.</p>\n","<p><code>levels</code> is <code>map(f -&gt; f[1]).distinct().toList()</code> — <code>distinct</code> keeps\nfirst-seen order for an ordered stream.</p>\n","<p><code>firstError</code> is <code>filter(...).map(f -&gt; f[3]).findFirst()</code>. Because it\nshort-circuits, nothing after the match is parsed.</p>\n","<p><code>countsByComponent</code> is\n<code>collect(Collectors.groupingBy(f -&gt; f[2], Collectors.counting()))</code>.</p>\n","<p><code>messageLengths</code> wants <code>mapToInt(f -&gt; f[3].length()).summaryStatistics()</code>.</p>\n","<p><code>valuesOf</code> needs <code>flatMap</code>: split the message on whitespace, keep the tokens\nstarting with <code>key + &quot;=&quot;</code>, and take the part after the <code>=</code>.</p>\n"],"solution":"static String[] fieldsOf(String line) {\n    return line.trim().split(\"\\\\s+\", 4);\n}\n\nstatic Stream<String[]> entries(List<String> lines) {\n    return lines.stream().map(Main::fieldsOf).filter(fields -> fields.length == 4);\n}\n\nstatic List<String> levels(List<String> lines) {\n    return entries(lines).map(fields -> fields[1]).distinct().toList();\n}\n\nstatic Optional<String> firstError(List<String> lines) {\n    return entries(lines)\n        .filter(fields -> fields[1].equals(\"ERROR\"))\n        .map(fields -> fields[3])\n        .findFirst();\n}\n\nstatic Map<String, Long> countsByComponent(List<String> lines) {\n    return entries(lines)\n        .collect(Collectors.groupingBy(fields -> fields[2], Collectors.counting()));\n}\n\nstatic IntSummaryStatistics messageLengths(List<String> lines) {\n    return entries(lines)\n        .mapToInt(fields -> fields[3].length())\n        .summaryStatistics();\n}\n\nstatic List<String> valuesOf(List<String> lines, String key) {\n    String prefix = key + \"=\";\n    return entries(lines)\n        .flatMap(fields -> Arrays.stream(fields[3].split(\"\\\\s+\")))\n        .filter(token -> token.startsWith(prefix))\n        .map(token -> token.substring(prefix.length()))\n        .toList();\n}","solutionNotesHtml":"<p>The shared prefix is the whole design. <code>entries</code> returns a <code>Stream&lt;String[]&gt;</code>,\nnot a <code>List&lt;String[]&gt;</code>, and that is deliberate: returning a stream keeps the\nlaziness, so <code>firstError</code> can still stop at the first match. Had <code>entries</code>\ncollected to a list, every caller would parse all eight lines whether or not it\nneeded them — the same mistake as <code>pipeline-order</code>’s\n<code>.toList().stream().findFirst()</code>, promoted to a helper method where it is\nharder to see.</p>\n<p>The one thing to be careful about is that <code>entries</code> returns a <em>fresh</em> stream on\neach call, because it starts from <code>lines.stream()</code>. A method returning a stored\n<code>Stream</code> field would fail the second time it was called with\n<code>IllegalStateException</code>. When you want a reusable pipeline, return something\nthat can build one — a <code>Supplier&lt;Stream&lt;T&gt;&gt;</code>, or just the source list, as here.</p>\n<p><code>split(&quot;\\\\s+&quot;, 4)</code> does the work that would otherwise need an index-hunting\nloop. The limit of 4 stops splitting after three separators, so the message\nkeeps its internal spaces; without it, <code>&quot;charge failed code=402&quot;</code> would arrive\nas three separate fields. The <code>filter(fields -&gt; fields.length == 4)</code> after it\nhandles blank lines and short lines in one place, which is why none of the five\nmethods has to think about them.</p>\n<p>The <code>filter</code> is a heuristic, and the tests say so: <code>&quot;any four words here&quot;</code> has\nfour whitespace-separated fields and is accepted as a log line, with <code>four</code> as\nits level. That is not a bug in the pipeline, it is the limit of the check you\nwere asked to write — and it is worth seeing, because “it parsed” is not the\nsame as “it was a log line”. A real reader would validate the timestamp, or\nmatch the level against a known set. The pipeline shape would not change; only\nthe predicate would.</p>\n<p><code>mapToInt(...).summaryStatistics()</code> gets the count, min, max, sum and average\nin a single pass with no boxing anywhere — the primitive-stream point from the\nchapter. The <code>Stream&lt;Integer&gt;</code> version would need either four passes or a\nhand-written reduction, and would allocate an <code>Integer</code> per line.</p>\n<p><code>valuesOf</code> shows <code>flatMap</code> doing what <code>map</code> cannot: the result has more\nelements than the input, because one line contributes two <code>user=</code> values in\nthis log and zero in another. Note it keeps duplicates — <code>&quot;ada&quot;</code> appears twice —\nbecause the specification asked for values, not distinct values. Adding\n<code>.distinct()</code> would be a one-word change, which is the argument for pipelines:\nthe shape of the answer is visible in the shape of the code.</p>\n"}